- hosts: lobby
  tasks:
    - name: Migrate Database
      become: true
      command: /usr/local/bin/run-flyway-migrate.sh

    - name: Pull Latest Lobby and Restart
      become: true
      command: /usr/local/bin/deploy-service.sh
